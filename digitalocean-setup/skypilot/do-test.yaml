name: do-test-cpu

resources:
  cloud: do
  cpus: 1+
  memory: 1+
  disk_size: 25

setup: |
  echo "Setting up DO instance..."

run: |
  echo "Hello from DigitalOcean!"
  uname -a
  lscpu | grep "Model name"
